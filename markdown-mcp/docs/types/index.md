# Type and contract index

- [`configuration-paths/`](configuration-paths/index.md) — immutable settings,
  authorized path records, fragments, and stable path failures.
- [`markdown-processing/`](markdown-processing/index.md) — parsed headings,
  front matter, spans, anchors, listings, and transformations.
- [`filesystem-mcp/`](filesystem-mcp/index.md) — snapshots, byte/result limits,
  atomic edit state, schemas, and MCP results.
